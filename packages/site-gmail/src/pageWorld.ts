// @darkly/site-gmail — Page-world script
// InboxSDK requires a page-world script to communicate with Gmail's page context.
// This is bundled as a separate entry point and loaded via web_accessible_resources.

import '@inboxsdk/core/pageWorld.js';
