/**
 * Screenshot generation pipeline entry point.
 *
 * This script will be implemented in issue #613 (Screenshot Pipeline).
 * It will:
 * 1. Build HTML pages via build-pages.ts
 * 2. Apply Darkly theme presets to each page
 * 3. Capture screenshots using Playwright
 * 4. Output to store-assets/ for CWS submission
 */

console.log('Screenshot pipeline not yet implemented.');
console.log('See issue #613 for the full pipeline implementation.');
console.log('Use "pnpm build:pages" to generate HTML mock pages.');
